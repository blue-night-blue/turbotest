<div>
  <% if quote.errors.any? %>
    <% quote.errors.each do |error| %>
        <%= error.full_message %>
    <% end %>
  <% end %>
</div>

<div>
  <%= form_with(model: quote) do |form| %>
      <%= form.text_field :name %>
      <%= form.submit %>
  <% end %>
</div>
